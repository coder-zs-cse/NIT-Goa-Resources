#include <iostream>
#define take(n) scanf("%d", &n);
#define rep(i, a, b) for (int i = a; i < b; i++)
int main()
{
    // int a=10,b=10,c=10;
    // int m=a++ + ++a;
    // int n=++b + b++;
    // int o=1+ ++c + c++;
    // printf("%d %d %d %d %d ",a,b,m,n,o);

    int p=10;
    printf("%d %d %d",++p,p);
    // float x = 100.1, y;
    // int *p;

    // p = (int *)&x; //explicit type
    // // conversion

    // y = *p;
    // printf("%d",*p);
    return 0;
}